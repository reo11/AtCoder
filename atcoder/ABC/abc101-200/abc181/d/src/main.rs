use proconio::input;

fn main() {
    input! {
        s: String
    }
    // 1000 = 8 * 125
    let mut counter = HashMap::new();
    for c in sentence.chars() {
        counter.insert(c as i64, )
    }
}